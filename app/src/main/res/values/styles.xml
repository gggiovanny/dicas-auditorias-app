<resources>

    <!-- Base application light theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="buttonStyle">@style/button</item>
        <item name="floatingActionButtonStyle">@style/floating_action_button</item>

        <!-- Adaptaciones para previsualizar en dark -->
        <item name="android:colorBackground">@color/background_surface_dark</item>
    </style>

    <style name="button" parent="@style/Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/colorPrimary</item>
    </style>

    <style name="card_item">

        <item name="android:layout_marginBottom">15dp</item>
        <item name="android:backgroundTint">@color/cardview_dark_background</item>
    </style>

    <style name="floating_action_button" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="backgroundTint">@color/yellow_dicas</item>
    </style>

</resources>
